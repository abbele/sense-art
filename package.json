{
  "name": "sense-art-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "SenseArt monorepo — library + demo apps",
  "author": "Michele Abbrescia",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' build",
    "dev:osd": "pnpm --filter=@sense-art/demo-osd dev",
    "typecheck": "pnpm -r typecheck",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "husky": "^9.1.7",
    "typescript": "~5.9.3"
  }
}
