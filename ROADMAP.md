# SenseArt — Roadmap

## Phase 0: Monorepo & Infrastructure

> Goal: Solid DX foundation before writing feature code.

- [x] Initialize pnpm workspace monorepo (`pnpm-workspace.yaml`)
- [x] Move library to `packages/sense-art/`
- [x] Create `apps/demo-osd/` — raw OSD + SenseArt demo (port 5173)
- [x] Create `apps/demo-storiiies/` — linear tour + SenseArt agency demo (port 5174)
- [x] Configure workspace root `package.json` with `build`, `dev:osd`, `dev:storiiies`, `typecheck` scripts
- [x] Fix `packages/sense-art/package.json` types entry (`dist/index.d.ts` ← generated by vite-plugin-dts)
- [x] Verify `pnpm typecheck` passes on all three packages
- [x] Set up Conventional Commits: `commitlint` + `@commitlint/config-conventional` + `husky` commit-msg hook
- [x] Define commit scope enum: `sense-art`, `demo-osd`, `demo-storiiies`, `ci`, `docs`, `deps`, `release`

### CI/CD Pipeline (GitHub Actions)

- [x] Create `.github/workflows/ci.yml` — typecheck + commitlint on every push and PR to `main`
- [x] Create `.github/workflows/release.yml` — semantic-release triggered on push to `main`
- [x] Create `packages/sense-art/.releaserc.json` — semantic-release config:
  - `@semantic-release/commit-analyzer` (Conventional Commits → version bump)
  - `@semantic-release/release-notes-generator`
  - `@semantic-release/changelog` (updates `CHANGELOG.md`)
  - `@semantic-release/npm` (publishes `sense-art` to npm)
  - `@semantic-release/git` (commits `CHANGELOG.md` + `package.json` back to repo)
  - `@semantic-release/github` (creates GitHub Release)
- [ ] Add `NPM_TOKEN` secret to GitHub repository Settings → Secrets → Actions
- [ ] Validate pipeline end-to-end with a `fix:` commit on `main`

---

## Phase 1: The Semantic Backbone

> Goal: A blind user can navigate the entire artwork spatially using only a keyboard and a screen reader.

### Task 1.1 — Project Setup & Focus Trap

- [x] Initialize Vite + Vanilla TypeScript project
- [x] Configure Library Mode (ESM + UMD output, `vite-plugin-dts`)
- [x] Install `openseadragon`, `tone`, `@types/openseadragon`, `vite-plugin-dts`
- [x] Update `package.json` with `exports`, `main`, `module`, `types`, `peerDependencies`
- [x] Create `tsconfig.build.json` for declaration emit
- [x] Write `README.md`, `ARCHITECTURE.md`, `ROADMAP.md`, `DOC_GUIDE.md`
- [x] Create `src/` skeleton: all placeholder classes with full type signatures and `@accessibility` JSDoc
- [x] Implement `SenseArtViewer` constructor — accepts `OpenSeadragon.Viewer` + `SenseArtOptions`
- [x] Implement `SenseArtViewer.mount()` / `unmount()` / `enable()` / `disable()` / `toggle()`
- [x] Implement `FocusTrap` — Arrow keys, `Tab`/`Shift+Tab` with wrap-around, `Escape` sentinel
- [x] Implement `Alt+A` global shortcut via `document` keydown listener
- [x] `pnpm typecheck` passes on all packages with zero errors
- [ ] Wire `activationShortcut` option from `SenseArtOptions` into `SenseArtViewer` (currently hardcoded to `Alt+A`)
- [ ] Implement `SenseArtViewer.setGrid(config)` — runtime grid resize without remount
- [ ] Set up Vitest + jsdom
- [ ] Write unit tests: `FocusTrap` activates/deactivates, keyboard events fire callbacks correctly

### Task 1.2 — Semantic Overlay Grid

- [x] Implement `A11yOverlay.render(getCellLabel)` — injects overlay above OSD canvas
- [x] Populate with `N×N` `<button role="gridcell">` (default 3×3) with `aria-rowindex`/`aria-colindex`
- [x] CSS: `position:absolute; pointer-events:none` on root; `pointer-events:auto` on cells
- [x] Dynamic `aria-label` per cell via `getCellLabel(row, col)` callback
- [x] Implement `A11yOverlay.destroy()` — clean DOM teardown
- [x] Implement `setCurrentCell()` — `aria-current="true"` on active cell
- [x] Implement `setRovingFocus()` — roving tabindex (single `tabindex="0"` at a time)
- [x] Visible focus ring (3px solid `#005fcc`) for sighted keyboard users
- [ ] **ARIA violation fix**: add `<div role="row">` wrappers — required by WAI-ARIA 1.2 Grid Pattern
- [ ] Write unit tests: ARIA roles/nesting, cell count, label content, `aria-current` toggling

### Task 1.3 — Coordinate Mapper

- [x] Implement `CoordinateMapper.cellToBounds()` — grid cell → normalized OSD `Rect`
- [x] Implement `focusToBounds()` — converts to viewport coordinates, calls `viewport.fitBounds()`
- [x] Implement `currentRegionLabel()` — Italian spatial labels for 3×3; numeric fallback for larger grids
- [x] Implement `currentZoomLabel()` — formatted zoom factor string
- [x] Implement `viewportToCell()` — live viewport center → `CellPosition` (used for `aria-current` sync)
- [ ] Sync overlay size/position with OSD container resize via `ResizeObserver`
- [ ] Write unit tests: bounds calculations for 3×3 and 5×5 grids, edge/wrap values

### Task 1.4 — ARIA-Live Engine

- [x] Implement `AriaLiveEngine` — injects visually hidden `aria-live="polite" aria-atomic="true"` into `<body>`
- [x] `announce(message, urgency)` with 150ms debounce
- [x] `announceZoom(zoomLevel, regionLabel)` — "Ingrandimento {n}x. Area: {region}"
- [x] `announceCell(row, col, label)` — "Riga {r}, Colonna {c} — {label}"
- [x] `destroy()` — removes region, clears pending debounce timer
- [ ] Write unit tests: message strings, debounce collapses rapid calls, `assertive` path

### Task 1.5 — Spatial Navigation

- [x] `Arrow` keys with wrap-around (columns and rows)
- [x] `Tab` / `Shift+Tab` linear cycle
- [x] `Escape` — `viewport.goHome()`, announces "Zoom reimpostato", focuses cell (0,0)
- [x] `aria-current="true"` updated on every cell focus
- [ ] `Enter` / `Space` — zoom into focused cell
- [ ] Write integration tests: full keyboard navigation end-to-end

---

## Phase 2: Sonification (Tone.js)

> Goal: A user can perceive color and texture information acoustically while navigating.

- [x] Define `PixelSampler` class — canvas sampling with luminosity/saturation computation
- [x] Define `Sonifier` class — `start()`, `stop()`, `mapToAudio(pixel)` with dynamic Tone.js import
- [ ] Create `src/ai/fixtures/artwork-map.json` (also needed by `MockProvider`)
- [ ] Wire `Sonifier` into `SenseArtViewer.onCellFocused` (behind `sonification.enabled` guard)
- [ ] Verify `PixelSampler.sample()` works on the live OSD canvas element
- [ ] Verify `Tone.Synth` + `Tone.Filter` mapping end-to-end in `demo-osd`
- [ ] `Sonifier.start()` called from within keyboard event handler (Web Audio user-gesture requirement)
- [ ] Write unit tests: luminosity→MIDI mapping, saturation→filter cutoff, audio context lifecycle

---

## Phase 3: AI Mental Map

> Goal: Each grid cell has a semantic label and sensory tags generated by a multimodal AI model.

### API Design (complete)

- [x] Define `ArtworkMapProvider` interface (pluggable, tree-shakeable)
- [x] Define `ArtworkMap` + `CellMetadata` types (`src/types/index.ts`)
- [x] Implement `MockProvider` — generates placeholder metadata without any API key
- [x] Implement `ArtworkMapClient` — fetch coordinator with `Map`-based cache and `clearCache()`
- [ ] Create `src/ai/fixtures/artwork-map.json` — hand-crafted 3×3 fixture for demo-storiiies

### Provider Implementations

| Provider | Model | Cost | Offline | Status |
|---|---|---|---|---|
| `MockProvider` | JSON fixture | Free | Yes | Done |
| `OpenAIProvider` | `gpt-4o` | Paid | No | Planned |
| `HuggingFaceProvider` | `llava-hf/llava-1.5-7b-hf` | Free tier | No | Planned |
| `HuggingFaceProvider` | `Salesforce/blip-2` | Free tier | No | Planned |
| `OllamaProvider` | `llava:13b` | Free | Yes | Planned |
| `GeminiProvider` | `gemini-1.5-flash` | Free tier | No | Planned |

- [ ] Evaluate label quality on a test artwork before picking a primary provider
- [ ] Implement `OpenAIProvider`
- [ ] Implement `HuggingFaceProvider`
- [ ] Implement `OllamaProvider`
- [ ] Wire `ArtworkMapClient` into `SenseArtViewer.mount()` — hydrate cell labels after image loads
- [ ] Document provider selection guide in `DOC_GUIDE.md`

---

## Phase 4: Testing & Release

- [ ] Complete Vitest + jsdom setup (Phase 1 unit tests)
- [ ] Set up axe-core for automated ARIA audits in CI
- [ ] VoiceOver (macOS/iOS) — full manual test against scripts in `DOC_GUIDE.md` (Tasks 1.2, 1.4)
- [ ] NVDA (Windows, Firefox) — integration test
- [ ] JAWS (Windows, Chrome) — compatibility check
- [ ] Add `NPM_TOKEN` secret to GitHub repository and validate `release.yml` end-to-end
- [ ] Publish `sense-art@1.0.0` to npm
- [ ] Write `CONTRIBUTING.md`
